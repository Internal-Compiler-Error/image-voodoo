use super::*;

impl CanvasImage {
    /// For each pixel, center at the origin, the width * height bounding box is used to calculate
    /// the min filtered value.
    pub fn apply_min_filer(&self, width: u32, height: u32) -> CanvasImage {

        todo!()
    }
}